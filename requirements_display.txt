# Display Manager Requirements for AI Assistant Robot
# Install with: pip install -r requirements_display.txt

# Core display library
pygame>=2.0.0

# Optional display enhancements (for better graphics on Pi)
numpy>=1.21.0

# For better font rendering (optional)
Pillow>=8.0.0

# Installation instructions:
# On Raspberry Pi, you may need to install pygame dependencies first:
# sudo apt-get update
# sudo apt-get install python3-pygame python3-dev libsdl2-dev libsdl2-image-dev libsdl2-mixer-dev libsdl2-ttf-dev

# For headless testing (without display):
# export SDL_VIDEODRIVER=dummy

# For fullscreen mode on Pi:
# Set fullscreen=True in DisplayManager initialization 